// Normalize common known variants that users might have typed incorrectly or with plural forms
    let cat = value.toString().trim().toLowerCase();
    // Map common aliases or plural forms to expected keys
    const categoryMap = {
        'uppers': 'upper',
        'lowers': 'lower',
        'shoe': 'shoes',
        'bags': 'bag',
        'accessories': 'accessory',
        'sock': 'socks'
    };
    if (categoryMap[cat]) {
        cat = categoryMap[cat];
    }
    return cat;
}
=======
function normalizeCategory(value = '') {
    let cat = value.toString().trim().toLowerCase();

    // Map common aliases or plural forms to expected keys
    const categoryMap = {
        'uppers': 'upper',
        'upper': 'upper',
        'lowers': 'lower',
        'lower': 'lower',
        'shoe': 'shoes',
        'shoes': 'shoes',
        'bags': 'bag',
        'bag': 'bag',
        'accessories': 'accessory',
        'accessory': 'accessory',
        'sock': 'socks',
        'socks': 'socks',
        'headwear': 'headwear',
        'eyewear': 'eyewear'
    };

    if (categoryMap[cat]) {
        cat = categoryMap[cat];
    } else {
        console.warn(\`WARNING: Unknown category '\${value}' normalized as '\${cat}'\`);
    }

    return cat;
}
>>>>>>> REPLACE

<<<<<<< SEARCH
// Modified to disable slide toggling since all slides are always visible
function showSlide(className) {
    // No operation: all slides always visible now
}

// Modified toggleColor to only update active class on buttons without hiding slides
function toggleColor(element) {
    if (activeElement && activeElement !== element) {
        activeElement.classList.remove('active');
    }

    if (activeElement !== element) {
        element.classList.add('active');
        activeElement = element;
        // no call to showSlide needed anymore
    }
}
=======
function showSlide(category) {
    Object.values(slides).forEach(slideId => {
        const slide = document.getElementById(slideId);
        if (slide) {
            slide.style.display = 'none';
        }
    });
    const activeSlide = document.getElementById(slides[category]);
    if (activeSlide) {
        activeSlide.style.display = 'block';
    }
}

function toggleColor(element) {
    if (activeElement && activeElement !== element) {
        activeElement.classList.remove('active');
    }
    if (activeElement !== element) {
        element.classList.add('active');
        activeElement = element;

        // Show the corresponding slide based on clicked tab class
        const classes = Array.from(element.classList);
        const categoryClass = classes.find(cls => Object.keys(slides).some(cat => cls.includes(cat)));
        if (categoryClass) {
            const normalizedCategory = normalizeCategory(categoryClass);
            if (normalizedCategory) {
                showSlide(normalizedCategory);
            }
        }
    }
}
